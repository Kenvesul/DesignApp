# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.coverage
htmlcov/
*.pyc

# ── Flask / DesignApp runtime ─────────────────────────────────────────────────
instance/
*.tmp
/tmp/

# ── Node / React ──────────────────────────────────────────────────────────────
react-spa/node_modules/
react-spa/.vite/
react-spa/dist/          # Built into ui/static/dist/ instead

# Built React SPA — committed only for production deploy; ignored during dev
ui/static/dist/

# ── Playwright ────────────────────────────────────────────────────────────────
playwright-report/
test-results/
.playwright/

# ── Environment secrets ───────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── IDE ────────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Generated exports (user-produced, not source) ─────────────────────────────
*.pdf
*.docx
!tests/fixtures/*.pdf
!tests/fixtures/*.docx

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/

# ── Append these lines to your existing .gitignore ──────────────────────

# Root-level test file (duplicate of tests/test_app.py — delete the file,
# or at minimum exclude it from git tracking)
/test_app.py

# Claude AI session context (moved to .claude/ — root copy no longer needed)
/CLAUDE_CONTEXT.md

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Flask session files (if using Flask-Session filesystem backend)
flask_session/

# Environment
.env
*.env.local

# React / Node
react-spa/node_modules/
react-spa/dist/
ui/static/dist/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp